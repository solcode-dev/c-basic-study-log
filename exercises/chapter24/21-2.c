#include <stdio.h>
#include <string.h>

int main(void)
{

  // 문제 1
  // char str[100];
  // int sum = 0;
  // fgets(str, 100, stdin);

  // for (int i = 0; str[i] != '\0'; i++)
  // {
  //   if (str[i] >= '0' && str[i] <= '9')
  //   {
  //     sum += str[i] - '0';
  //   }
  // }
  // printf("%d\n", sum);

  // 문제 2
  // char str1[20];
  // char str2[20];
  // char str3[50];

  // fgets(str1, 20, stdin); // simple\n
  // fgets(str2, 20, stdin); // string\n

  // str1[strlen(str1) - 1] = 0;
  // str2[strlen(str2) - 1] = 0;

  // strncpy(str3, str1, strlen(str1)); //
  // strncat(str3, str2, strlen(str2));
  // printf("%s", str3);

  return 0;
}